# 08 - Expand Memory Typology

## Priority

Wave 2: close content gaps.

## Status

Done.

## Problem

The source critique mentions memory gaps, but the document does not visualize or develop the memory model.

## Task

Add a memory typology section and diagram.

## Memory Types

- Working memory.
- Episodic memory.
- Semantic memory.
- Procedural memory.

## Acceptance Criteria

- [x] The document explains all four memory types.
- [x] The diagram shows how each memory type is used by an agent.
- [x] The section distinguishes memory from context window, retrieval, and logs.
- [x] The section connects memory to state/current context, RAG/knowledge, cross-session history, and skills/procedures.

## Notes

- Added a dedicated `Memory typology` section to `agent-ontology.html` after the 2022-2026 timeline.
- Expanded memory into working, episodic, semantic, and procedural memory with a practical architecture mapping table.
- Added a compact SVG diagram showing how each memory type connects to the agent harness and context window.
- Separated memory from context window, retrieval, and logs in a boundary table.
- Removed the old short `Memory — отдельная подсистема` gap from the source-gaps section and renumbered the remaining final sections to `§12` and `§13`.
